sd_to_rub = 80.38

amount_usd =float(input("Введите сумму в долларах: "))

def convert_usd_to_rub(amount_usd):
    return amount_usd * usd_to_rub

amount_rub = convert_usd_to_rub(amount_usd)

print(f"Cумма в рублях: {amount_rub:,.2f}".replace(".", ",") + f" руб")
